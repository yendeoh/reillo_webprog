const express = require('express');
const router = express.Router();

// Placeholder article routes — replace with real controllers when available
router.get('/', (req, res) => {
  res.json({ articles: [] });
});

module.exports = router;
